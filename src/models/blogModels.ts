export type blogURImodel = {
    id: string
}

export type blogCreateModel = {
    name: string,
    description: string,
    websiteUrl: string
}

export type blogUpdateModel = {
    name: string,
    description: string,
    websiteUrl: string
}

export type blogViewModel = {
    id: string,
    name: string,
    description: string,
    websiteUrl: string
}

